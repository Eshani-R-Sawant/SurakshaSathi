# --- Core service ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
tenacity==8.5.0

# --- Data stores (plain DSN connections -- works against any Postgres/Redis, not GCP-specific) ---
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
pgvector==0.3.5
redis==5.0.8
pybloom-live==4.0.0

# --- ML / NLP ---
# CPU-only torch wheel -- sentence-transformers pulls torch transitively, and without this index
# pip resolves the default PyPI wheel, which is the CUDA build (1-2GB+, slow to fetch/install on a
# server that never uses a GPU). This one shrinks the wheel to ~200-300MB and cuts build time a lot.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
scikit-learn==1.7.2
pandas==2.2.3
numpy==2.1.1
xgboost==2.1.1
langdetect==1.0.9
sentence-transformers==3.1.1
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.6
langchain-text-splitters==0.3.0

# --- APK static analysis ---
androguard==4.1.2

# --- Document / image processing ---
pypdf==6.14.2
opencv-python-headless==4.10.0.84
pyzbar==0.1.9
cryptography==43.0.1

# --- External API clients ---
google-cloud-translate==3.16.0
groq==0.11.0
twilio==9.3.2
python-whois==0.9.4
firebase-admin==6.5.0
tavily-python==0.5.0

# --- Web / crawling ---
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
