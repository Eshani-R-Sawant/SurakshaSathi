<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG-ONLY network security config (app/src/debug/res/xml). Gradle's debug source set
    automatically overrides the main res/xml/network_security_config.xml of the same name for
    debug builds only; release builds always get the strict main version, never this one.

    Allows cleartext HTTP to local dev-server addresses only, so BACKEND_BASE_URL can point at a
    developer's own machine while iterating (10.0.2.2 is the Android emulator's fixed alias for
    the host loopback interface; localhost/127.0.0.1 cover a physical device reached via
    adb reverse). Every other host still requires HTTPS, same as release.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
