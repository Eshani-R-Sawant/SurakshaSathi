# Core ML
torch>=2.1
transformers>=4.40
safetensors>=0.4
datasets>=2.18
accelerate>=0.28
scikit-learn>=1.4

# Data
pandas>=2.0
numpy>=1.26
pyyaml>=6.0

# Clustering (auto-installed by cluster_spam.py if missing)
sentence-transformers>=3.0

# Web UI
flask>=3.0

# URL intelligence
requests>=2.31
tldextract>=5.0
python-Levenshtein>=0.21
jellyfish>=1.0

# QR decode (web-UI demo fallback only; Android decodes on-device via ML Kit)
opencv-python-headless>=4.9

# Mobile export: TFLite conversion + quantization
ai-edge-torch>=0.7
ai-edge-litert>=2.1
onnx>=1.15

# LLM backends (install only the one you use)
# anthropic>=0.25       # pip install anthropic
# openai>=1.30          # pip install openai
# google-generativeai>=0.5  # pip install google-generativeai
